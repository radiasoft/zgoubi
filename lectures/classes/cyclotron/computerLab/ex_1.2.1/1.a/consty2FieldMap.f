C Will read X, Y, BZ from zgoubi.plt (filled up using CONSTY), will organize 
C these data and store as a 2D field map (Z==0) in consty2FieldMap.out.
C This field map is usable with TOSCA[MOD=25].
C Could be extended to provide 3D mad, for use with TOSCA[MOD=24].

